if(LOSCFG_ARCH_ARM_AARCH32)
    add_subdirectory(arm)
elseif(LOSCFG_ARCH_ARM_AARCH64)
    add_subdirectory(arm64)
elseif(LOSCFG_ARCH_XTENSA)
    add_subdirectory(xtensa)
elseif(LOSCFG_ARCH_RISCV)
    add_subdirectory(riscv)
else()
    message(FATAL_ERROR "cpu arch not implement !")
endif()
